<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Estoque</title>

    <style>
        /* Deixa tudo zerado antes do codigo rodar*/
        * {
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
        }

         /* Estilos para o corpo da página (a tela inteira) */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            background: linear-gradient(140deg, #e3fdf5 1%, #bbfbe6 99%); 
            min-height: 100vh; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            position: relative; 
        }

        /* Estilos do cartão branco de login (Container Principal) */
        .login-container {
            background: white; 
            border-radius: 16px; 
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1); 
            padding: 40px; 
            width: 100%; 
            max-width: 400px; 
            transition: transform 0.3s ease, box-shadow 0.3s ease; 
            z-index: 10; 
        }

        /* --- NOVOS ESTILOS --- */

        /* Estilização do Cabeçalho (Logo e Título) */
        .login {
            text-align: center; 
            margin-bottom: 30px; 
        }

        .logo h1 {
            color: #2d3436; 
            font-size: 24px; 
            margin-bottom: 8px; 
        }

        .logo p {
            color: #636e72; 
            font-size: 14px;
        }

        /* Estilização dos Grupos de Formulário (Label + Input) */
        .formgrupo {
            margin-bottom: 20px; 
        }

        label {
            display: block; 
            margin-bottom: 8px; 
            color: #2d3436;
            font-weight: 600; 
            font-size: 14px;
        }

        /* Estilização Comum para Inputs de Texto e Senha */
        .input-usuario input, 
        .input-senha input {
            width: 100%; 
            padding: 12px 15px; 
            border: 2px solid #dfe6e9; 
            border-radius: 8px; 
            font-size: 15px;
            transition: all 0.3s ease; 
            outline: none; 
        }

        /* Efeito quando clica no input (Foco) */
        .input-usuario input:focus, 
        .input-senha input:focus {
            border-color: #00b894; 
            box-shadow: 0 0 0 3px rgba(0, 184, 148, 0.1); 
        }

        /* Estilos Específicos para o Campo de Senha */
        .input-senha {
            position: relative; 
        }

        /* Botão de Olho (Ver/Ocultar Senha) */
        .password-toggle {
            position: absolute; 
            right: 15px; 
            top: 50%; 
            transform: translateY(-50%); 
            background: none; 
            border: none; 
            cursor: pointer; 
            font-size: 18px; 
            color: #636e72; 
        }

        .password-toggle:hover {
            color: #2d3436; 
        }

        /* Estilo do link Esqueci Minha Senha */
        .esqueci-wrapper {
            text-align: right; 
            margin-top: -10px; 
            margin-bottom: 15px; 
        }
        
        .link-esqueci {
            color: #00b894; 
            font-size: 13px;
            text-decoration: none; 
            font-weight: 600;
            cursor: pointer;
        }

        .link-esqueci:hover {
            text-decoration: underline; 
        }

        /* Botão de Login (Entrar) */
        .login-btn {
            width: 100%; 
            padding: 14px; 
            background-color: #00b894; 
            color: white; 
            border: none; 
            border-radius: 8px; 
            font-size: 16px; 
            font-weight: bold; 
            cursor: pointer;
            transition: background-color 0.3s ease; 
            margin-top: 10px; 
        }

        /* Efeito Hover do Botão */
        .login-btn:hover {
            background-color: #019577; 
        }

        /* Estado Desabilitado */
        .login-btn:disabled {
            background-color: #b2bec3; 
            cursor: not-allowed; 
        }

        /* --- CSS DO RODAPÉ --- */
        .rodape-mosqueteiros {
            position: fixed; 
            bottom: 15px;    
            left: 0;         
            width: 100%;     
            text-align: center; 
            color: #535c68;  
            font-size: 13px; 
            font-weight: 500; 
            z-index: 1;      
            opacity: 0.8;    
        }
    </style>
</head>
<body>
    <div class="login-container">
    
        <div class="login">
            <div class="logo">
                <h1>Controle de estoque</h1>
                <p>Realize o login para entrar</p>
            </div>
        </div>

        <form id="loginForm" action="#">
            <div class="formgrupo">
                <label for="username">Usuario</label>
                <div class="input-usuario">
                    <input type="text" id="username" name="username" required autocomplete="username" placeholder="Digite seu usuário">
                </div>
            </div>

            <div class="formgrupo">
                <label for="password">Senha</label>
                <div class="input-senha">
                    <input type="password" id="password" name="password" required autocomplete="current-password" placeholder="Digite sua senha">
                    <button type="button" class="password-toggle" onclick="togglePassword()">👁</button> 
                </div>
            </div>

            <div class="esqueci-wrapper">
                <a class="link-esqueci" id="btnEsqueciSenha">Esqueci minha senha</a>
            </div>
            
            <button type="submit" class="login-btn">
                <span class="btn-text">Entrar</span>
            </button>
        </form>
    </div>

    <footer class="rodape-mosqueteiros">
        <p>Desenvolvido e idealizado por Mosqueteiros</p>
    </footer>

    <script>
        // --- FUNÇÃO VISUAL: Mostrar/Ocultar Senha ---
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const toggleBtn = document.querySelector('.password-toggle');

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text'; 
                toggleBtn.textContent = '🙈'; 
            } else {
                passwordInput.type = 'password';
                toggleBtn.textContent = '👁';
            }
        }

        // --- LÓGICA REAL: ESQUECI MINHA SENHA ---
        document.getElementById('btnEsqueciSenha').addEventListener('click', async function(e) {
            e.preventDefault();
            
            // 1. Pede o email ao usuário
            const email = prompt("Digite seu e-mail para receber o link de recuperação:");

            // 2. Se o email não estiver vazio, tenta enviar para o Backend
            if (email && email.trim() !== "") {
                try {
                    // CONFIGURAÇÃO: Endereço do seu Backend Java (Ajuste a rota se precisar)
                    const response = await fetch('http://localhost:8080/api/recuperar-senha', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ email: email })
                    });

                    // 3. Verifica a resposta do servidor
                    if (response.ok) {
                        alert("Sucesso! Verifique sua caixa de e-mail.");
                    } else if (response.status === 404) {
                        alert("Este e-mail não está cadastrado no sistema.");
                    } else {
                        alert("Erro ao tentar enviar o e-mail. Tente novamente.");
                    }

                } catch (erro) {
                    console.error("Erro técnico:", erro);
                    alert("Erro ao conectar com o servidor. O sistema está online?");
                }
            }
        });

        // --- LÓGICA REAL: LOGIN ---
        document.getElementById('loginForm').addEventListener('submit', async function(e){
            e.preventDefault(); // Previne recarregamento da página

            // Elementos de UI
            const button = document.querySelector('.login-btn');
            const btnText = document.querySelector('.btn-text');
            const usuarioInput = document.getElementById('username').value;
            const senhaInput = document.getElementById('password').value;
            
            // 1. Feedback visual de "Carregando"
            const textoOriginal = btnText.innerText;
            btnText.innerText = "Conectando...";
            button.disabled = true;

            try {
                // 2. Envia os dados para o Backend Java
                // CONFIGURAÇÃO: Endereço do seu Backend Java
                const response = await fetch('http://localhost:8080/api/login', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ 
                        usuario: usuarioInput, 
                        senha: senhaInput 
                    })
                });

                // 3. Processa a resposta
                if (response.ok) {
                    // Se login for aprovado (status 200)
                    // Pega o token ou dados que o Java retornou (opcional)
                    const data = await response.json(); 
                    
                    alert("Login realizado com sucesso! Redirecionando...");
                    
                    // 4. Redireciona para a página principal do sistema
                    window.location.href = "/dashboard.html"; // Mude para o nome da sua página principal

                } else if (response.status === 401 || response.status === 403) {
                    alert("Usuário ou senha incorretos!");
                } else {
                    alert("Ocorreu um erro no servidor. Tente mais tarde.");
                }

            } catch (erro) {
                // Erro de conexão (ex: servidor desligado)
                console.error("Erro de conexão:", erro);
                alert("Erro ao conectar com o servidor. Verifique se o Java está rodando.");
            } finally {
                // 5. Restaura o botão, independente se deu certo ou errado
                btnText.innerText = textoOriginal;
                button.disabled = false;
            }
        });
    </script>
</body>
</html>